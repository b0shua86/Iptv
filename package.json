{
  "name": "mac-iptv-server",
  "version": "1.0.0",
  "description": "Self-hosted IPTV server for a Mac mini: serves Free-TV live channels plus your local media to LG / Fire TV / any IPTV player, with a built-in web player and channel guide.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check src/config.js && node --check src/livePlaylist.js && node --check src/mediaLibrary.js && node --check src/playlist.js && node --check src/epg.js && node --check src/stream.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "license": "MIT"
}
